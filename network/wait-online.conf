# Boot waits for one working link, not every link (Wi-Fi is often idle on this desktop).
# Installed as /etc/systemd/system/systemd-networkd-wait-online.service.d/dotfiles.conf.
[Service]
ExecStart=
ExecStart=/usr/lib/systemd/systemd-networkd-wait-online --any --timeout=20
