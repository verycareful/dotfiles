# NetworkManager: never hand a network's DHCP DNS to resolved, so Ethernet and every Wi-Fi
# use the global AdGuard servers only. /etc/resolv.conf is resolved's stub (a symlink).
# Installed as /etc/NetworkManager/conf.d/10-dotfiles-dns.conf by install-network.sh.
[main]
dns=none
rc-manager=unmanaged
