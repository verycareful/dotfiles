# exec-once runs once at launch; exec runs on every config reload.
# waybar/quickshell/hypridle/… as user units (systemd restarts them if they crash); see session-start
exec-once = ~/.local/bin/session-start
exec-once = awww-daemon --no-cache
exec-once = ~/.local/bin/wall set          # waits for awww-daemon itself
exec-once = wl-paste --type text  --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
exec-once = easyeffects --gapplication-service
